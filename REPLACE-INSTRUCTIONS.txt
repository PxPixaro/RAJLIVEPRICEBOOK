V82.9 LIVE VIEW BY GRID + PDF FIX

Replace only:
1) /index.html
2) /js/app.js

Master Excel remains only:
/data/price-book.xlsx

Do NOT recreate /assets/data.

Fix: after live Excel is loaded, FAST_ROWS is rebuilt synchronously before filters/grid render. Previously a sparse same-length FAST_ROWS array could make the live Excel refresh fail during applyFilters(), leaving the old bundled hierarchy visible. Grid and PDF both continue to use viewByFields().
